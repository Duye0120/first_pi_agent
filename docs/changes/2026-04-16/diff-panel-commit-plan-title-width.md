# Diff Panel 提交标题宽度修正

**时间**: 2026-04-16 19:24

## 改了什么

1. 把提交计划卡片顶部改成单独的信息行。
2. 把标题输入框移到下一行，独占整行宽度。

## 为什么改

- 用户反馈标题输入区当前宽度太窄，换行前就被压缩，观感很差。
- 标题属于主信息，应该直接占满卡片主体宽度。

## 涉及文件

- `src/renderer/src/components/assistant-ui/diff-panel.tsx`

## 结果

- 标题输入区现在会按卡片宽度撑满后再换行。
