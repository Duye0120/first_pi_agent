# 修复 shell-main 左侧内容列未撑满导致的底部空白

**时间**: 2026-04-16 17:18

## 改了什么

1. 给 `shell-main` 内部左侧内容包裹层补上了 `flex h-full flex-col`。

## 为什么改

- 右侧工作区改成 panel-only 后，左侧内容列少了一层纵向撑满约束。
- 主内容只按自身内容高度渲染，底部留下了一整块空白区。

## 涉及文件

- `src/renderer/src/App.tsx`

## 结果

- 聊天主内容会重新撑满可用高度。
- 底部那块空白区域会消失。
